//import Search from "../Search/Search";

const A = ({ children }) => {
  return (
    <div className="wrapper">
      {/* <Search></Search> */} {children}
    </div>
  );
};

export default A;
