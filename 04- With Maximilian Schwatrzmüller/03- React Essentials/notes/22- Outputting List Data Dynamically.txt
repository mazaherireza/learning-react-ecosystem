Outputting List Data Dynamically
--------------------------------
... I don't mean the visuals, but the underlying code.
... it would be better if the number of CoreConcept components would be derived dynamically based on the number of items ...

... an array of JSX elements.

... JSX is capable of dealing with arrays of renderable data, array of JSX code, ...
... transform arrays in JS with help of the built-in map method, ...

... what we previously manually accessed by index step by step, can be accessed automatically with help of map method.

... key ... it's a special prop, understood and used by React
... can be added to any component ... built-in elements and to custom components ...
... unique ... it's used by React to efficiently render and update list ...